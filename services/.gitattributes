# 自動でテキスト判定
* text=auto

# 改行コードはLFに統一
* text eol=lf

# TypeScript, JSON は必ずテキスト扱い
*.ts   text
*.js   text
*.json text

# シェルスクリプトはLF
*.sh text eol=lf

# バイナリファイル（画像など）は自動除外
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary