{
  "name": "services",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "cdk",
    "lambda"
  ],
  "scripts": {
    "build:cdk": "cd cdk && npm run build",
    "build:lambda": "cd lambda && npm run build",
    "build": "npm run build:cdk && npm run build:lambda",
    "test": "jest --config jest.config.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^18.11.18",
    "aws-cdk": "^2.1016.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
